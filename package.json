{
  "name": "trading-agent-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Trading Agent Monorepo workspace",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:agent": "npm run dev -w agent-server",
    "dev:desktop": "npm run start -w trading-agent",
    "dev": "npm run dev:desktop"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
